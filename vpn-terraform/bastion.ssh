
lsblk
sudo mkfs.ext4 /dev/sdb
sudo mkdir /mnt/persisten_disk
sudo mount /dev/sdb /mnt/persisten_disk
sudo chmod 777 /mnt/persisten_disk
echo "It exist" > first.txt

mkdir vpn_modules
mkdir vpn_config