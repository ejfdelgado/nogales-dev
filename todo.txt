
Use nginx to serve videocall
Use static local IP in GCE videocall

How to pass custom version to scripts of package.json

Use ssh in nginx

Allow the cloud run be visible from other cloud run instances, get in into the GCE to ping the cloud run end point.
