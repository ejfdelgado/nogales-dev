# docker compose --profile all up -d
# docker compose --profile all down --remove-orphans
# docker compose logs -f mysql
# docker compose logs -f wordpress

# docker exec -it 1ce6d52f5331 sh
# vi /etc/nginx/http.d/default.conf
# docker commit 020d0882fa3d us-central1-docker.pkg.dev/local-volt-431316-m2/nogales/wordpress:3.0.7

# http://localhost:8080/

volumes:
  mysql_data:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: "./data/mysql_data"
  wordpress_data:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: "./data/wordpress_data"

networks:
  network_wordpress:
    name: network_analitics
    driver: bridge
    enable_ipv6: false

services:
  mysql:
    profiles: [ "all" ]
    image: mysql:8.4
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      network_wordpress:
        aliases:
          - mysql

  wordpress:
    profiles: [ "all" ]
    #image: wordpress:php8.2-apache
    image: ${WORDPRESS_IMAGE}
    ports:
      - 8080:8080
    environment:
      PORT: 8080
      WORDPRESS_DB_HOST: mysql
      WORDPRESS_DB_USER: ${MYSQL_USER}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD}
      WORDPRESS_DB_NAME: ${MYSQL_DATABASE}
      MS_OAUTH_CLIENT_ID: ${MS_OAUTH_CLIENT_ID}
      MS_OAUTH_CLIENT_SECRET: ${MS_OAUTH_CLIENT_SECRET}
      MS_OAUTH_TENANT_ID: ${MS_OAUTH_TENANT_ID}
    volumes:
      - wordpress_data:/var/www/html
    networks:
      network_wordpress:
        aliases:
          - wordpress

